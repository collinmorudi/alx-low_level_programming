#include "main.h"

/**
 * wildcmp - compares two strings
 * @s1: first string
 * @s2: second string
 * Return: 1 if s1 and s2 are identical, else 0
 */

int wildcmp(char *s1, char *s2)
{
	if (s1 == s2)
	{
		return (1);
	}
	return (1);
}
