#include "main.h"
#include <stddef.h>

/**
 * strtow - add a desc
 * @str: a string to work with
 * Return: a pointer
 */

char **strtow(char *str)
{
	if (str == NULL || str == "")
		return (Null);
	return (NULL);
}
