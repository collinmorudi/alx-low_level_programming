#include "main.h"
#include <stddef.h>

/**
 * argstostr - add a desc
 * @ac: a string to work with
 * @av: to add
 * Return: a pointer
 */

char *argstostr(int ac, char **av)
{
        if (ac == NULL || av == NULL)
                return (NULL);
        return (NULL);
}
